// This page generates the results using code+state from the authorize

type SpotifyAuthParams = {
  code: string;
  state: string;
}

export default function ResultsPage({ searchParams }: { searchParams: SpotifyAuthParams }) {
  
}